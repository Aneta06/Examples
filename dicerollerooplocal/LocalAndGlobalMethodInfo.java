package dicerollerooplocal;

public class LocalAndGlobalMethodInfo {

    // local = declare inside a method visible only to that method
    //       = deklarovat uvnitř metody viditelné pouze pro tuto metodu


    // global = declared outside a method but within a class visible to all parts of class
    //        = deklarován mimo metodu, ale v rámci třídy viditelné pro všechny části třídy
}
